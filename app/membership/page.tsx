import { Wrapper } from "@/components/membership/wrapper";

export default async function Membership() {
    return (
      <div className="flex w-full h-screen bg-blue-500">
        <Wrapper/>
      </div>
    );
}
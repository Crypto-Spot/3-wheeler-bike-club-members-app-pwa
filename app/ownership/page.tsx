import { Wrapper } from "@/components/ownership/wrapper";

export default async function Ownership() {

    return (
      <div className="flex w-screen h-screen">
        <Wrapper /> 
      </div>
    );
}
import { Wrapper } from "@/components/sponsorship/wrapper";

export default async function Sponsorship() {
    return (
      <div className="flex w-full h-screen">
        <Wrapper />
      </div>
    );
}